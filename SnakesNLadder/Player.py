class Player:
    def __init__(self, player_id, current_position):
        self.id = player_id
        self.current_position = current_position

# # Example usage
# player_instance = Player(player_id="p1", current_position=0)
#
# # Accessing player attributes
# print(f"Player ID: {player_instance.id}")
# print(f"Current Position: {player_instance.current_position}")
